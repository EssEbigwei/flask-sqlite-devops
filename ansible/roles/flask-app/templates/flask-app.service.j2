 [Unit]
   Description=Flask SQLite Application
   After=network.target

   [Service]
   User=ec2-user
   WorkingDirectory={{ app_dir }}
   ExecStart=/usr/local/bin/gunicorn --workers 3 --bind 0.0.0.0:5000 app:app
   Restart=always

   [Install]
   WantedBy=multi-user.target
